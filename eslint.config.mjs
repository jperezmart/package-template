// SCAFFOLDING — a starting point, not canon. Nothing in the standard depends on
// this file, on ESLint existing, or on the shared config below: replace it with
// whatever your repo actually wants.
//
// It consumes `@jperezmart/eslint-config` rather than inlining rules, which is
// what that package is for — every plugin it registers is its own dependency,
// so there is nothing else to install alongside it.
import base from '@jperezmart/eslint-config/base';

export default base;
