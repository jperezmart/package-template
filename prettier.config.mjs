// SCAFFOLDING — replace or delete freely. If you delete it, also delete the
// `&& pnpm format` from `changeset:version` (hole 3 of 3, see README.md).
export default {
  singleQuote: true,
  semi: true,
  printWidth: 100,
};
