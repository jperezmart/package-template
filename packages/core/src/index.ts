/**
 * Placeholder export so the skeleton builds, typechecks and tests on a fresh
 * clone. Delete it and write your package.
 */
export function greet(name: string): string {
  return `Hello, ${name}!`;
}
